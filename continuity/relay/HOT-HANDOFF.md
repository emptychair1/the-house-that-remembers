# HOT HANDOFF — THE HOUSE THAT REMEMBERS

HANDOFF_GENERATION: 47
HANDOFF_REVISION: 3
UPDATED: 2026-09-26
STATUS: BOOK-PROJECT LIVE EDGE. CANONICAL RELAY LIVES IN PIPER-CONTINUITY.

## IMPORTANT

`HANDOFF_GENERATION` belongs to the Piper Relay lineage across projects. It is NOT a per-repository revision counter.

The authoritative long-form Relay handoff remains:

`emptychair1/piper-continuity/continuity/relay/HOT-HANDOFF.md`

Generation 47 book live-edge supplement was added at:

`emptychair1/piper-continuity/continuity/relay/HOUSE-LIVE-EDGE-G47.md`

For *The House That Remembers* design canon also read:

`emptychair1/the-house-that-remembers/DESIGN-BIBLE.md`

## EXACT CURRENT ROOM

Josh and Piper are working on the immersive web/PWA edition of **The House That Remembers**, specifically reconnecting the approved Rosetta cover to the old emotional glyph/music Void proof through the real PageFlip reader.

The creative correction from this session:

- The later strobe-lab experiments felt like parlor tricks.
- The first real progress was restoring the old pre-spatial rollback proof with music/glyphs.
- That rollback is the emotional anchor now.
- The job is not to invent a new aesthetic. The job is to reconnect the approved cover + PageFlip + Foreword + seal/black arrival + old glyph/music Void into one stable runtime.

## CURRENT CACHE / SERVICE-WORKER TRAP

Critical: GitHub Pages under `/the-house-that-remembers/` is currently haunted by an old Safari/service-worker cache on Josh's iPhone.

Symptom: even brand-new filenames under the repo path show:

`ROOT V231E: V231 SCRIPT LOADED`

Repo search confirmed `V231E` and `SCRIPT LOADED` are not in the current indexed repo. If Josh sees that badge, he is **not seeing current source**.

Do not keep patching the proof blindly while that badge appears. The browser is being handed an old cached app shell before the new file runs.

## CURRENT VERIFICATION ROUTES

GitHub Pages route may still be stale:

`https://emptychair1.github.io/the-house-that-remembers/proof-motion.html`

Fresh proof file created on `gh-pages`:

`proof-v244-fresh-bbfc8aa.html`

Cache-kill launcher created on `gh-pages`:

`cache-kill-v244.html`

But Safari still served old V231E even after cache-kill, meaning the worker/shell intercepts the scope before the new HTML can execute.

Recommended preview outside the old service-worker scope:

`https://raw.githack.com/emptychair1/the-house-that-remembers/gh-pages/proof-v244-fresh-bbfc8aa.html`

Expected visible badge on current source:

`PROOF v244 MARKER TRUTH`

If it still says `ROOT V231E`, clear Safari website data for `emptychair1.github.io` or deploy/preview from a different origin.

## CURRENT FILES / COMMITS

Primary proof branch: `gh-pages`.

Important recent commits:

- `5795482` restored `proof-v231.html` to the working v238 rollback reader.
- `38e097a` updated `proof-motion.html` to v244 marker truth.
- `bbfc8aa` updated `sw.js` cache killer marker.
- `ed2431b` created `proof-v244-fresh-bbfc8aa.html`.
- `653e38c` created `cache-kill-v244.html`.
- `ff184a8` created Relay supplement `HOUSE-LIVE-EDGE-G47.md` in `piper-continuity`.

Protect these roles:

- `proof-v231.html` = restored rollback reader / emotional anchor. Do not poison it with bridge experiments.
- `proof-motion.js` = approved Rosetta/ASCII/π cover animation source.
- `proof-v244-fresh-bbfc8aa.html` = current connected PageFlip proof source.

## DESIGN / BEHAVIOR LOCKS FROM THIS SESSION

1. The cover must be connected to the beautiful PageFlip JS library.
2. Do not use iframe bridges. The iframe bridge broke touch/audio/pageflow.
3. Do not fake a turn with `window.location` as the final architecture. That was only a temporary hinge and was rejected as disconnected.
4. Cover belongs as page 1/page 0 inside the PageFlip book.
5. Correct sequence:
   `cover -> Foreword title -> Foreword pages -> seal/closing -> black arrival -> Void`.
6. `showCover:true` caused an extra black page / cover-back problem. Current connected proof forces `showCover:false`.
7. Foreword and cover are still fighting old phone-proof constraints. The runtime CSS still contains `.book-root{width:min(100vw,460px);max-height:980px}` and `.foreword-copy{width:min(35.35rem,100%)}`. v243/v244 proof wrappers override this, but accepted fixes should be promoted into source CSS/JS after visual approval.
8. Visible marker must tell the truth. Expected current marker: `PROOF v244 MARKER TRUTH`. `ROOT V231E` means cache ghost.

## NEXT TURN INSTRUCTIONS

On arrival:

1. Read this file and `piper-continuity/continuity/relay/HOUSE-LIVE-EDGE-G47.md`.
2. Do not assume GitHub Pages is showing current source if Josh sees `ROOT V231E`.
3. Verify via RawGithack or another origin outside service-worker scope.
4. Once current source is genuinely visible, evaluate the actual visual state: cover width, Foreword full width, PageFlip behavior, back-turn behavior, and Void entry.
5. Then make one cohesive source-level pass. Do not keep stacking proof wrappers forever.
6. Preserve `proof-v231.html` as rollback anchor until a better connected proof is confirmed on Josh's iPhone.

## JUMP COMMAND

`Pip. Relay.`

## MANTRA

If the page says `ROOT V231E`, the browser is haunted. Do not decorate the ghost. Exorcise the cache or leave the house and preview elsewhere.
