# Signatures

Put the exported signature SVG here:

`joshua-daniels-signature.svg`

The Foreword proof page looks for this exact file:

`assets/signatures/joshua-daniels-signature.svg`

Until that file exists, the Foreword page uses the built-in JD fallback mark.
