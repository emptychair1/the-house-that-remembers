const canvas=document.getElementById('c'),ctx=canvas.getContext('2d',{alpha:false});const img=new Image();img.src='./assets/source/IMG_3301.png';const PI='31415926535897932384626433832795028841971693993751058209749445923078164062862089986280348253421170679';let glyphs=[],start=0,dpr=1,W=0,H=0;const ease=t=>t<.5?4*t*t*t:1-Math.pow(-2*t+2,3)/2,clamp=(v,a=0,b=1)=>Math.max(a,Math.min(b,v));function resize(){dpr=Math.min(devicePixelRatio||1,2);W=innerWidth;H=innerHeight;canvas.width=W*dpr;canvas.height=H*dpr;ctx.setTransform(dpr,0,0,dpr,0,0);build()}function build(){if(!img.complete||!img.naturalWidth)return;glyphs=[];const cell=Math.max(4,Math.min(6,W/75)),font=Math.max(5,cell*1.22),scale=Math.min(W/img.naturalWidth,H/img.naturalHeight),dw=img.naturalWidth*scale,dh=img.naturalHeight*scale,ox=(W-dw)/2,oy=(H-dh)/2,off=document.createElement('canvas'),oc=off.getContext('2d');off.width=Math.max(1,Math.floor(dw/cell));off.height=Math.max(1,Math.floor(dh/(cell*1.35)));oc.drawImage(img,0,0,off.width,off.height);const data=oc.getImageData(0,0,off.width,off.height).data;let n=0;for(let y=0;y<off.height;y++)for(let x=0;x<off.width;x++){const k=(y*off.width+x)*4,lum=(data[k]*.2126+data[k+1]*.7152+data[k+2]*.0722)/255;if(lum<.045)continue;const tx=ox+(x+.5)*cell,ty=oy+(y+.5)*cell*1.35,ang=Math.random()*Math.PI*2,rad=Math.max(W,H)*(.18+Math.random()*.75);glyphs.push({tx,ty,x:tx+Math.cos(ang)*rad,y:ty+Math.sin(ang)*rad,ch:PI[n++%PI.length],lum:clamp(Math.pow(lum,.42)*1.42),delay:Math.random()*1.9,font,phase:Math.random()*6.28})}}function lightning(t){let a=0;if(t>2.55&&t<2.67)a=Math.sin((t-2.55)/.12*Math.PI);if(t>4.42&&t<4.49)a=Math.max(a,Math.sin((t-4.42)/.07*Math.PI));if(t>4.57&&t<4.66)a=Math.max(a,.58*Math.sin((t-4.57)/.09*Math.PI));if(a){ctx.save();ctx.globalCompositeOperation='screen';ctx.fillStyle=`rgba(255,255,255,${a*.82})`;ctx.fillRect(0,0,W,H);ctx.restore()}}function headlights(t){const p=clamp((t-1.75)/3.2);if(!p)return;const y=H*(1.12-.36*ease(p)),spread=W*(.13+.08*p);ctx.save();ctx.globalCompositeOperation='screen';for(const sx of[-1,1]){const x=W*.5+sx*spread,g=ctx.createRadialGradient(x,y,0,x,y,W*.42);g.addColorStop(0,`rgba(255,255,255,${.13*p})`);g.addColorStop(.25,`rgba(255,255,255,${.07*p})`);g.addColorStop(1,'rgba(255,255,255,0)');ctx.fillStyle=g;ctx.fillRect(0,0,W,H)}ctx.restore()}function frame(ts){if(!start)start=ts;const t=(ts-start)/1000;ctx.fillStyle='#000';ctx.fillRect(0,0,W,H);headlights(t);ctx.textAlign='center';ctx.textBaseline='middle';for(const g of glyphs){const p=ease(clamp((t-.25-g.delay)/4.25));if(p<=0)continue;const alive=p>.985?Math.sin(t*.9+g.phase)*.32:0,x=g.x+(g.tx-g.x)*p+alive,y=g.y+(g.ty-g.y)*p+alive*.45,alpha=clamp((.18+.82*p)*g.lum);ctx.font=`300 ${g.font}px ui-monospace,SFMono-Regular,Menlo,monospace`;ctx.fillStyle=`rgba(255,255,255,${alpha})`;ctx.fillText(g.ch,x,y)}lightning(t);if(t>7.2){ctx.fillStyle=`rgba(0,0,0,${clamp((t-7.2)/1.4)})`;ctx.fillRect(0,0,W,H)}if(t>9){start=ts;build()}requestAnimationFrame(frame)}img.onload=()=>{resize();addEventListener('resize',resize);requestAnimationFrame(frame)};